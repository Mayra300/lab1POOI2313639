public class E1
{
  public static void main (String[]args)
  {
    // Declaramos las variables enteras x e y
    int x = 144;
    int y = 999;

    // Mostramos por pantalla el valor de cada variable
      System.out.println ("El valor de x es: " + x);
      System.out.println ("El valor de y es: " + y);

    // Declaramos las variables realizamos las operaciones matemeticas
    int suma = x + y;
    int resta = x - y;
    float division = (float) x / y;
    int multiplicacion = x * y;

    // Mostramos los resultados por pantalla
      System.out.println ("La suma de x e y es: " + suma);
      System.out.println ("La resta de x e y es: " + resta);
      System.out.println ("La division de x entre y es: " + division);
      System.out.println ("La multiplicacion de x e y es: " + multiplicacion);
  }
}
